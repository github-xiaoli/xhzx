一、新增配置文件 config/query_mode.json
在仓库根目录创建 config 文件夹，放入此文件。

json
{
  "mode": "open"
}
模式说明：

"open" — 只需输入 8 位学号即可查看违纪总分与明细。

"strict" — 需输入学号 + 姓名（姓名必须与违纪记录中的姓名完全匹配）才可查看。
页面加载时静默读取此文件，不会向用户提示当前模式。